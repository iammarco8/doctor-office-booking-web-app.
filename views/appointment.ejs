<%- include('./partials/header'); %>
<table class="table table-dark">
    <thead>
        <tr>
            <th>appointment id</th>
            <th> Name</th>
            <th>age</th>
            <th>doctor</th>
            <th>specialty</th>
        </tr>
    </thead>        
    <% for (let i=0; i < appointments.length; i++){ %>
        <tbody>
            <tr class="table-active">
                <td><%= appointments[i].id %></td>
                <td><%= appointments[i].patient_first %> <%= appointments[i].patient_last %></td>
                <td><%= appointments[i].age %></td>
                <br>
                <td><%= appointments[i].first_name %> <%= appointments[i].last_name %></td>
                <td><%= appointments[i].specialty %></td>
                <td>
                    <a href="/appointment/<%= appointments[i].id %>">view</a>
                </td>
            </tr>
            <!-- <h2><% doctor.value %></h2> -->
            
            <% } %>
            <tr>
                <td>
                    <a href="/makeApointmnet">Create appointment</a>
                </td>
            </tr>
        </tbody>
    </table>
<%- include('./partials/footer'); %>